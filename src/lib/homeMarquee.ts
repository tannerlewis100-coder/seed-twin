export const MARQUEE_MESSAGES = [
  "Free shipping over $150",
  "Public COA Library",
  "Research Use Only",
] as const;

/** Number of copies of the message set rendered in the scrolling track. */
export const MARQUEE_REPEATS = 6;
