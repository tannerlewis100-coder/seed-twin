Update contact details on `/contact` page.

1. Change email from `support@clarum.com` to `clarumpeps@gmail.com` in the `contactInfo` array.
2. Change location from `Brooklyn, NY` to `Keller, Texas` in the same array.

File: `src/routes/contact.tsx`